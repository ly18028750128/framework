spring:
  application:
    group:
    name: ${spring.application.group:}eureka-client # 对应config-server所获取的配置文件的{application}
  security:
    user:
      name: anxminise
      password: 123456
  cloud:
    config:
      uri: http://localhost:8040/
      name: eureka-client-tester
      profile: dev # 指定profile，对应config-server所获取的配置文件中的{profile}
      label: master # 指定git仓库的分支，对应config-server所获取的配置文件的{label}
  profiles:
    active: dev
  datasource:
    url: jdbc:mysql://127.0.0.1:13306/framework?userSSL=false&useUnicode=true&characterEncoding=UTF8&serverTimezone=GMT
    username: framework
    password: 1qaz@WSX
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
management:
  endpoints:
    web:
      base-path: /monitor